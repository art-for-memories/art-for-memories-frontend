import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", 'res.cloudinary.com', 'cdn.rareblocks.xyz', 'www.newtimes.co.rw'],
  },
};

export default nextConfig;
